## 目的

<!-- このPRで達成すること。対応するIssue番号があれば記載する -->

## 変更内容

<!-- 変更したファイル・機能の概要 -->

## テスト（TDD: plan → red test → coding → green test）

- [ ] red test を先に書いた
- [ ] green test（`npm run test`）が通ることを確認した
- [ ] フロントエンドの確認を行った（該当する場合）：`curl` / `wget --mirror` / Playwright のいずれか

## 非エンジニア向けユーザーテスト

<!--
専門知識のない来場者・クライアントが、実際に画面を操作して変更を確認できる手順を
丁寧に書くこと。「〇〇ボタンを押す」「△△と表示されることを確認する」のように、
専門用語を避けて具体的に書く。空欄のままではマージできません（pr-checker がチェックする）。

実施方法：Claude Desktop の sandbox > chrome を使用する。認証機能を持つ画面では
ログインを要求する（本デモは認証を持たないため、セッションCookieが正しく発行される
ことの確認をもって代替する）。
-->

1.
2.
3.

## セキュリティレビュー

- [ ] コミット前にセキュリティレビュー（OWASP10 / QC10 観点）を実施した
- [ ] 秘密情報（`.env` の値、`config/master.key` 等）をコミットしていないことを確認した
- [ ] 削除コマンド（`rm` 等）を含んでいないことを確認した
- [ ] `alert()` / `confirm()` / `prompt()` を使用していない

## ドキュメント

- [ ] `README.md` / `SPEC/` に、今回実装した範囲だけを追記した（未実装のものは書いていない）
- [ ] 連絡先を書く場合は `info@rictaworks.jp` のみを使用し、個人名を書いていない
- [ ] コンテンツはですます調で書いている（である調を使っていない）

## ブランチ運用

- [ ] `src/**` を変更しているため、このPRを経由している（`main` への直接pushではない）
