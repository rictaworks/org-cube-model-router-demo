# app-ui

デザインモック配置場所。現時点ではモック未提供。

- 事前にデザイン指定が届いたら、このディレクトリにモックを配置する
- `app-ui/` は参照専用（デザイン確認用）で、実装のEdit scopeには含めない
- アプリ実装時は、ここのモックを `apps/web/src/` に再現したうえで実際のAPIに接続する
  （[CONTRIBUTING.md](../CONTRIBUTING.md) 参照）
