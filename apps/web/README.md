# apps/web

フロントエンド（Cloudflare Pages）。現時点では未着手。

- 実装コードは `apps/web/src/` 配下に置く（開発用スクリプトは `src/` の外）
- デザイン指定がある場合は `app-ui/` のモックを `src/` に再現したうえで API に接続する（[CONTRIBUTING.md](../../CONTRIBUTING.md)）
- 実装したページは、実装したPRの中で [README.md](../../README.md) のページ一覧に追記する
