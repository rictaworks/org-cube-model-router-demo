/**
 * packages/router-core エントリポイント（雛形）
 *
 * requirements.md 4章の関数A〜F（次元管理・有効ポリシー解決・候補評価・
 * 割当決定・再計算・固定割当）は、以降のissueで本ファイルおよび同階層に
 * 関数として実装する。外部I/O（DB・HTTP）を持たない純粋関数とし、
 * apps/api から呼び出す。
 *
 * 現時点では雛形構築のみのため、公開エクスポートは持たない。
 */
export {};
